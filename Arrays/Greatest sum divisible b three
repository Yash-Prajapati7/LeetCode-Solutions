Leetcode Question : [greatest-sum-divisible-by-three](https://leetcode.com/problems/greatest-sum-divisible-by-three/)

### Java

```java
class Solution {

    public int maxSumDivThree(int[] nums) {
        int[] bestSumByRemainder = { 0, Integer.MIN_VALUE, Integer.MIN_VALUE };

        for (int num : nums) {

            int[] updatedBest = new int[3];
            System.arraycopy(bestSumByRemainder, 0, updatedBest, 0, 3);

            int remainder = num % 3;

            for (int r = 0; r < 3; r++) {
                int newRemainder = (r + remainder) % 3;
                updatedBest[newRemainder] = Math.max(
                    updatedBest[newRemainder],
                    bestSumByRemainder[r] + num
                );
            }

            bestSumByRemainder = updatedBest;
        }

        return bestSumByRemainder[0];
    }
}
```

### C++

```cpp
#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int maxSumDivThree(vector<int>& nums) {
        vector<int> bestSumByRemainder = {0, INT_MIN, INT_MIN};

        for (int num : nums) {

            vector<int> updatedBest = bestSumByRemainder;
            int remainder = num % 3;

            for (int r = 0; r < 3; r++) {
                int newRemainder = (r + remainder) % 3;
                updatedBest[newRemainder] = max(
                    updatedBest[newRemainder],
                    bestSumByRemainder[r] + num
                );
            }

            bestSumByRemainder = updatedBest;
        }

        return bestSumByRemainder[0];
    }
};
```
